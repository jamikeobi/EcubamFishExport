import { ImageHeaderDirective } from './image-header.directive';

describe('ImageHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
